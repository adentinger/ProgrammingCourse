// NOTE: Les lignes qui ont des erreurs font arrêter
// l'exécution du fichier. Une fois que l'on a constaté qu'elles
// causent  un erreur, les supprimer ou modifier puis recharger la
// page pour que les lignes suivantes puissent s'exécuter.

// Suggestion de tests:
// - Comment mettre ("assigner") une valeur dans une variable?
let a;
a = 1.1;
console.log(a); // Affiche 1.1
let b = 1.2;
console.log(b); // Affiche 1.2

// - Que se passe-t-il si on remplace le contenu d'une variable?
let c = 2.1;
console.log(c); // Affiche 2.1
c = "So long, and thanks for all the fish!";
console.log(c); // Affiche So long, and thanks for all the fish!,
                // donc le contenu se fait remplacer

// - Est-ce qu'utiliser une variable change sa valeur?
let d = 3.1;
console.log(d); // Affiche 3.1
console.log(d); // Affiche aussi 3.1, donc une variable conserve sa
                // valeur jusqu'à ce qu'on lui en mette explicitement
                // une autre.

// - Que se passe-t-il si on affiche une variable dans laquelle
//     on n'a rien assigné?
// À faire

// - Peut-on déclarer deux variables du même nom?
// À faire

// - Est-ce que la casse (majuscules versus minuscules) est
//     prise en compte? Autrement dit, est-ce que "a" et "A" sont
//     la même variable?
// À faire

// - Quels caractères sont valides dans le nom d'une variable? Par
//     exemple, est-ce que le nom d'une variable peut commencer par
//     un chiffre?
// À faire

// - Est-ce qu'on peut copier le contenu d'une variable dans une
//     autre variable?
// À faire
